{
  "devDependencies": {
    "jest": "^24.7.1",
    // @if babel
    "babel-jest": "^24.7.1",
    // @endif
    // @if typescript
    "@types/jest": "^24.0.11",
    "ts-jest": "^24.0.2",
    // @endif
  },
  "scripts": {
    "pretest": "npm run lint",
    "test": "jest"
  }
}
