{
  "devDependencies": {
    "ava": "^2.2.0",
    // @if !aurelia
    "browser-env": "^3.2.6",
    // @endif
    // @if babel
    "@babel/register": "^7.5.5",
    // @endif
    // @if typescript
    "ts-node": "^8.3.0",
    // @endif
  },
  "scripts": {
    "pretest": "npm run lint",
    "test": "ava"
  }
}
