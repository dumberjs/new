{
  "devDependencies": {
    "ava": "^2.4.0",
    // @if !aurelia
    "browser-env": "^3.2.6",
    // @endif
    // @if babel
    "@babel/register": "^7.6.2",
    // @endif
    // @if typescript
    "ts-node": "^8.4.1",
    // @endif
  },
  "scripts": {
    "pretest": "npm run lint",
    "test": "ava"
  }
}
