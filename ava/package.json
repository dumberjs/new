{
  "devDependencies": {
    "ava": "^3.15.0",
    // @if !aurelia
    "browser-env": "^3.3.0",
    // @endif
    // @if babel
    "@ava/babel": "^1.0.1",
    "@babel/register": "^7.12.10",
    // @endif
    // @if typescript
    "@ava/typescript": "^1.1.1",
    "ts-node": "^9.1.1",
    // @endif
  },
  "scripts": {
    "pretest": "npm run lint",
    "test": "ava"
  }
}
