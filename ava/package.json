{
  "devDependencies": {
    "ava": "^3.3.0",
    // @if !aurelia
    "browser-env": "^3.3.0",
    // @endif
    // @if babel
    "@babel/register": "^7.8.3",
    // @endif
    // @if typescript
    "ts-node": "^8.6.2",
    // @endif
  },
  "scripts": {
    "pretest": "npm run lint",
    "test": "ava"
  }
}
