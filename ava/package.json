{
  "devDependencies": {
    "ava": "^3.3.0",
    // @if !aurelia
    "browser-env": "^3.3.0",
    // @endif
    // @if babel
    "@ava/babel": "^1.0.1",
    "@babel/register": "^7.8.3",
    // @endif
    // @if typescript
    "@ava/typescript": "^1.1.0",
    "ts-node": "^8.6.2",
    // @endif
  },
  "scripts": {
    "pretest": "npm run lint",
    "test": "ava"
  }
}
