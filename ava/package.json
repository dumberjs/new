{
  "devDependencies": {
    "ava": "^1.4.1",
    // @if !aurelia
    "browser-env": "^3.2.6",
    // @endif
    // @if babel
    "@babel/register": "^7.4.0",
    // @endif
    // @if typescript
    "ts-node": "^8.1.0",
    // @endif
  },
  "scripts": {
    "pretest": "npm run lint",
    "test": "ava"
  }
}
