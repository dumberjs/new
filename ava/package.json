{
  "devDependencies": {
    "ava": "^3.15.0",
    // @if !aurelia
    "browser-env": "^3.3.0",
    // @endif
    // @if babel
    "@ava/babel": "^2.0.0",
    "@babel/register": "^7.15.3",
    // @endif
    // @if typescript
    "@ava/typescript": "^2.0.0",
    "ts-node": "^9.1.1",
    // @endif
  },
  "scripts": {
    "pretest": "npm run lint",
    "test": "ava"
  }
}
