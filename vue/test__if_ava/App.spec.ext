import test from 'ava';
import { mount } from '@vue/test-utils';
import App from '../src/App';

test('should render message', t => {
  const wrapper = mount(App);
  t.is(wrapper.text(), 'Hello Vue!');
});
