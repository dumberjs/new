import React from "react";
// @if css
import "./styles.css";
// @endif
// @if less
import "./styles.less";
// @endif
// @if sass
import "./styles.scss";
// @endif

const App = () => (
  <div className="app">
    <h1>Hello React!</h1>
  </div>
);

export default App;
